language: python

python:
  - "3.6"

jobs:
  include:
    - stage: test
      python: 
        - "3.6"
      install:
        - pip install -r requirements.txt
      script:
        - cd tests
        - pytest

